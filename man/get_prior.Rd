% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_prior.R
\name{get_prior}
\alias{get_prior}
\title{Construct Prior Parameters for Beta Kernel Process (BKP)}
\usage{
get_prior(
  prior = c("noninformative", "fixed", "adaptive"),
  r0 = 2,
  p0 = 0.5,
  y = NULL,
  m = NULL,
  K = NULL
)
}
\arguments{
\item{prior}{Type of prior: one of \code{"noninformative"}, \code{"fixed"}, or
\code{"adaptive"}.}

\item{r0}{Global precision parameter (used in \code{"fixed"} and
\code{"adaptive"}).}

\item{p0}{Global prior mean (used in \code{"fixed"} prior only; must be in
(0,1)).}

\item{y}{A numeric vector of observed successes.}

\item{m}{A numeric vector of total trials at each location.}

\item{K}{A precomputed kernel matrix (m x n), typically from
\code{kernel_matrix()}.}
}
\value{
A list with two numeric vectors: \code{alpha0} and \code{beta0} (each of length
m).
}
\description{
Constructs prior Beta distribution parameters (alpha0 and beta0)
at each location based on user-specified prior type: noninformative, fixed,
or data-adaptive.
}
\examples{
# Simulated data
set.seed(123)
n <- 10
X <- matrix(runif(n * 2), ncol = 2)
y <- rbinom(n, size = 5, prob = 0.6)
m <- rep(5, n)

# Example kernel matrix (Gaussian)
K <- kernel_matrix(X)

# Construct prior (adaptive)
prior <- get_prior(prior = "adaptive", r0 = 2, y = y, m = m, K = K)
str(prior)

}
